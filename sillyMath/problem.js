function add(num1, num2) {

// convert both numbers into arrays of strings

// find which array is longer

// starting with the longer array loop backward

// while indexes exist and the two digits together
// push the result into a results array


// if the longer array still has digits just append those as is to the front of the result

  return num1 + num2;
  
}


module.exports = add;